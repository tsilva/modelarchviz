# Credential references only. Store values with `keyenv set ENV_NAME`.
[keyenv]
version = 1

[secrets.OPENROUTER_API_KEY]
account = "modelarchviz/OPENROUTER_API_KEY"
required = true

[secrets.SENTRY_AUTH_TOKEN]
account = "modelarchviz/SENTRY_AUTH_TOKEN"
required = true
